services:
  nginx-test:
    image: nginx:1.29.3
    container_name: rooday-nginx
    restart: unless-stopped
  traefik-test:
    image: traefik:3.6.2
    container_name: rooday-traefik
    command:
      - "--api.insecure=true"
      - "--providers.docker=false"
    restart: unless-stopped
