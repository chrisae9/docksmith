services:
  cadvisor-test:
    image: gcr.io/cadvisor/cadvisor:v0.47.0
    container_name: rooday-cadvisor
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    restart: unless-stopped
    privileged: true
