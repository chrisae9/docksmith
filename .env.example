# Docksmith Environment Variables
# Copy this file to .env and adjust as needed

# Database path (default: /data/docksmith.db)
DB_PATH=/data/docksmith.db

# Background check interval - how often to scan for updates
# Use Go duration format: 5m, 1h, 24h, etc.
CHECK_INTERVAL=1h

# Cache TTL - how long to cache registry responses
# Reduces API calls to registries
CACHE_TTL=1h

# GitHub token for accessing private GHCR images (optional)
# Create at: https://github.com/settings/tokens
# Required scopes: read:packages
GITHUB_TOKEN=

# Configuration file path (optional)
# CONFIG_PATH=/data/docksmith.yaml

# UI static files directory (default: /app/ui/dist)
# Only change if running outside Docker
# STATIC_DIR=/app/ui/dist
